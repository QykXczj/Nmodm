# ==================== 核心依赖（必需） ====================

# GUI 框架 - 用于所有界面组件、窗口、信号槽机制
# 锁定版本以确保本地和 GitHub Actions 编译环境一致
PySide6==6.10.0

# HTTP 请求库 - 用于下载工具、GitHub API 调用、版本检测
requests>=2.31.0

# 系统进程管理 - 用于进程查找/终止、父进程检测、进程监控
psutil>=5.9.0

# TOML 配置文件解析 - 用于 EasyTier 和 ME3 mod 配置文件
toml>=0.10.2

# 加密库 - 用于存档转换功能和安全相关操作
cryptography>=41.0.0


# ==================== Windows 特定依赖（可选） ====================

# Windows API - 用于获取文件版本信息和注册表访问
# 代码中有 try/except 处理，不安装也能运行，但部分功能受限
pywin32>=306

# PE 文件解析 - 用于备用的版本检测方法
# 可选依赖，有降级方案
pefile>=2023.2.7


# ==================== 打包工具（开发用） ====================

# Nuitka 编译器 - 高性能打包工具（推荐用于生产环境）
# 锁定版本以确保本地和 GitHub Actions 编译环境一致
nuitka==2.8.3

# PyInstaller 打包工具 - 备用打包方案
pyinstaller>=6.0.0

# 图像处理库 - Nuitka 处理图标文件时需要
imageio>=2.31.0


# ==================== 代码质量工具（开发用） ====================

# 代码格式化工具
black>=23.0.0

# 代码检查工具
flake8>=6.0.0

# 类型检查工具
mypy>=1.0.0


# ==================== 测试工具（开发用） ====================

# 测试框架
pytest>=7.0.0

# 测试覆盖率
pytest-cov>=4.0.0

# PySide6 测试支持
pytest-qt>=4.0.0
